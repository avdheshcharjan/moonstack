# Thetanuts API endpoint
API_URL=https://round-snowflake-9c31.devops-118.workers.dev/

# Server port (optional, defaults to 3001)
PORT=3000

# Database URL for Supabase (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=""

# Supabase Service Role Key (for server-side database access)
# Find this in your Supabase project settings > API > service_role key
# WARNING: Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Supabase Public Keys (safe for client-side)

NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Vercel Cron Secret (for authenticating cron requests)
# Generate with: openssl rand -base64 32
CRON_SECRET=""

# Referrer Address (your unique platform identifier for tracking positions)
# This address is used when calling fillOrder() to identify your platform
# Filter user positions by this address to show only trades from your platform
REFERRER_ADDRESS=""
NEXT_PUBLIC_URL=""
# Coinbase Developer Platform - Paymaster Configuration
# Get your credentials from https://portal.cdp.coinbase.com

# Paymaster API endpoint (Base network)
NEXT_PUBLIC_PAYMASTER_URL=""

# Bundler API endpoint (Base network)
NEXT_PUBLIC_BUNDLER_URL=""

# CDP Project credentials (keep these secret - server-side only)
CDP_PROJECT_ID=""
CDP_API_KEY=""


# Network configuration
NEXT_PUBLIC_CHAIN_ID=8453